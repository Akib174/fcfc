# 🗺️ fcfc — একদম শুরু থেকে ডিপ্লয় করার সম্পূর্ণ গাইড (বাংলায়)

> ধরে নেওয়া হয়েছে আপনি আগে কখনো এরকম ডিপ্লয় করেননি। প্রতিটি ধাপে বলা আছে **কোন কমান্ড চালাবেন, কোথায় ক্লিক করবেন, কী কপি করবেন**। কোনো ধাপ স্কিপ করবেন না।

---

## ⚡ দ্রুততম উপায় (এক কমান্ডে সব — নিচের লম্বা গাইড লাগবে না)

রুট ফোল্ডারের **`deploy.mjs`** স্ক্রিপ্ট সবকিছু নিজে করে দেয়: D1/KV/R2 তৈরি, `wrangler.toml`-এ আইডি বসানো, মাইগ্রেশন, সিক্রেট জেনারেশন (JWT/VAPID), ফ্রন্টএন্ড বিল্ড, ডিপ্লয় ও লাইভ টেস্ট — **অ্যাপ ও API একটাই URL-এ** (আলাদা Pages সাইট/CORS লাগে না)।

1. **Node.js 18+** ইনস্টল থাকতে হবে (`node -v` দিয়ে দেখুন)।
2. টোকেন বানান: **dash.cloudflare.com → My Profile → API Tokens → Create Token → "Edit Cloudflare Workers"** টেমপ্লেট নিন → Permissions অংশে গিয়ে আরও দুটো যোগ করুন: **D1 → Edit** এবং **Workers R2 Storage → Edit** → Create Token → টোকেন কপি।
   (ইতিমধ্যে বানানো টোকেন হলে: API Tokens তালিকায় টোকেনের পাশে **⋯ → Edit** → পারমিশন যোগ → Update Token — টোকেনের মান বদলায় না।)
3. টার্মিনালে প্রজেক্ট ফোল্ডারে গিয়ে:

```bash
# macOS / Linux:
export CLOUDFLARE_API_TOKEN="আপনার-টোকেন"
node deploy.mjs

# Windows PowerShell:
$env:CLOUDFLARE_API_TOKEN="আপনার-টোকেন"
node deploy.mjs
```

শেষে একটাই URL পাবেন (যেমন `https://fcfc-backend.<সাবডোমেইন>.workers.dev`) — ব্রাউজারে খুলে সাইনআপ করলেই চলবে। ভয়েস/ভিডিও কল চাইলে: ড্যাশবোর্ড → Calls → API টোকেন বানিয়ে `CALLS_API_TOKEN="টোকেন" node deploy.mjs` আবার চালান।

- স্ক্রিপ্টটি **ইডিম্পোটেন্ট** — বার বার চালানো যায়।
- কোনো সমস্যা হলে `node deploy.mjs --dry-run` দিয়ে ক্লাউড ছাড়া চেক করতে পারেন।
- একাধিক ক্লাউডফ্লেয়ার অ্যাকাউন্ট থাকলে `CLOUDFLARE_ACCOUNT_ID="আইডি"` ও সেট করুন।

> ম্যানুয়ালি ধাপে ধাপে করতে চাইলে (বা কী ঘটছে বুঝতে চাইলে) নিচের সম্পূর্ণ গাইড দেখুন।

---

## 📋 সামগ্রিক রোডম্যাপ (যা যা ঘটবে)

```
ধাপ ০ → কম্পিউটারে প্রয়োজনীয় সফটওয়্যার ইনস্টল
ধাপ ১ → জিপ ফাইল খোলা
ধাপ ২ → টার্মিনাল খোলা ও ফোল্ডারে যাওয়া
ধাপ ৩ → ক্লাউডফ্লেয়ার অ্যাকাউন্ট ও লগইন
ধাপ ৪ → ব্যাকএন্ডের রিসোর্স বানানো (ডেটাবেজ, স্টোরেজ)
ধাপ ৫ → সিক্রেট পাসওয়ার্ডগুলো সেট করা
ধাপ ৬ → ব্যাকএন্ড ডিপ্লয় (একটি ইউআরএল পাবেন)
ধাপ ৭ → ফ্রন্টএন্ডে ওই ইউআরএল বসানো
ধাপ ৮ → ফ্রন্টএন্ড ডিপ্লয় (আরেকটি ইউআরএল পাবেন)
ধাপ ৯ → টেস্ট করা
ধাপ ১০ → (ঐচ্ছিক) নিজের ডোমেইন যুক্ত করা
```

---

## ধাপ ০: কম্পিউটারে যা যা ইনস্টল লাগবে

### ০.১ — নোড-জেএস (Node.js)

1. ব্রাউজারে যান: **https://nodejs.org**
2. সবুজ বাটনে ক্লিক করুন — **"LTS"** লেখা যেটা (ডানদিকের কোনো "Current" নয়)।
3. ডাউনলোড হওয়া ফাইল চালিয়ে **Next → Next → Install** দিয়ে ইনস্টল করুন (কিছু পরিবর্তন করার দরকার নেই)।
4. ইনস্টল শেষ হলে **কম্পিউটার রিস্টার্ট করা ভালো** (না করলেও বেশিরভাগ সময় কাজ করে)।

### ০.২ — টার্মিনাল খোলা শিখে নিন

- **উইন্ডোজ**: স্টার্ট মেনুতে লিখুন `PowerShell` → Enter। অথবা কোনো ফোল্ডারের ভেতরে **Shift চেপে ধরে রাইট-ক্লিক** → "Open PowerShell window here"।
- **ম্যাক**: Spotlight (Cmd+Space) এ লিখুন `Terminal` → Enter।
- **লিনাক্স**: Ctrl+Alt+T।

টার্মিনালে যা লেখা হয় সেটা "কমান্ড" — লিখে **Enter** চাপলে সেটা চলে।

### ০.৩ — যাচাই করুন নোড ইনস্টল হয়েছে কি না

টার্মিনালে লিখুন:

```bash
node -v
npm -v
```

দুটোতেই ভার্সন নম্বর দেখালে (যেমন `v20.11.0`) — ঠিক আছে। এরকম না দেখালে নোড আবার ইনস্টল করুন।

### ০.৪ — কোড এডিটর (ফাইল এডিট করার জন্য)

**https://code.visualstudio.com** থেকে **VS Code** ইনস্টল করুন (ফ্রি)। এটা দিয়েই `wrangler.toml`, `.env` ফাইলগুলো এডিট করবেন। (নোটপ্যাড দিয়েও পারবেন, তবে এটা অনেক সহজ।)

---

## ধাপ ১: জিপ ফাইল খোলা

1. `fcfc.zip` ফাইলটি যেখানে পেয়েছেন সেখানে **রাইট-ক্লিক** করুন।
2. **Extract All...** (উইন্ডোজ) বা **Open / Extract** (ম্যাক) ক্লিক করুন।
3. এমন একটা জায়গায় রাখুন যেখানে সহজে যাওয়া যায়, যেমন:
   - উইন্ডোজ: `C:\fcfc`
   - ম্যাক/লিনাক্স: `~/fcfc` (হোম ফোল্ডার)

খোলার পর ভেতরে এমন দেখাবেন:

```
fcfc/
├── README.md
├── DEPLOYMENT-GUIDE.md   ← এই ফাইলটিই
├── worker/               ← ব্যাকএন্ড কোড
└── frontend/             ← ফ্রন্টএন্ড কোড
```

---

## ধাপ ২: টার্মিনালে ফোল্ডারে যাওয়া

টার্মিনাল খুলে লিখুন (আপনার জিপ কোথায় খুলেছেন সেই পথ):

**উইন্ডোজে `C:\fcfc` হলে:**
```powershell
cd C:\fcfc
```

**ম্যাক/লিনাক্সে হোম ফোল্ডারে হলে:**
```bash
cd ~/fcfc
```

> 💡 সহজ ট্রিক: টার্মিনালে `cd ` লিখে স্পেস দিয়ে, এরপর ফোল্ডারটি টার্মিনাল উইন্ডোতে **টেনে ছেড়ে দিন** — পথ নিজেই লেখা হয়ে যাবে।

এখন লিখুন:
```bash
ls
```
(`worker` ও `frontend` ফোল্ডার দেখতে পেলে আপনি ঠিক জায়গায় আছেন। উইন্ডোজে `ls` কাজ না করলে `dir` লিখুন।)

---

## ধাপ ৩: ক্লাউডফ্লেয়ার অ্যাকাউন্ট + লগইন

### ৩.১ — অ্যাকাউন্ট খোলা

1. ব্রাউজারে যান: **https://dash.cloudflare.com/sign-up**
2. ইমেইল + পাসওয়ার্ড দিয়ে সাইনআপ করুন। (ফ্রি প্ল্যানই যথেষ্ট — কোনো কার্ড লাগবে না।)
3. ইমেইল ভেরিফাই করুন।

### ৩.২ — টার্মিনাল থেকে লগইন

টার্মিনালে লিখুন:

```bash
npm install -g wrangler
```
(এটা একটু সময় নিতে পারে, চা খান ☕)

এবার:

```bash
wrangler login
```

- ব্রাউজার **নিজে নিজে খুলবে** এবং ক্লাউডফ্লেয়ার লগইন পেজ দেখাবে।
- লগইন করলে একটা পেজ আসবে — সেখানে **"Allow"** বাটনে ক্লিক করুন।
- টার্মিনালে ফিরে দেখবেন লেখা: `Successfully logged in.` ✅

> সমস্যা হলে: ব্রাউজার না খুললে টার্মিনালে যে লিংকটা দেখাবে সেটা কপি করে নিজে ব্রাউজারে পেস্ট করুন।

---

## ধাপ ৪: ব্যাকএন্ড রিসোর্স বানানো

এখন থেকে **সব কমান্ড `worker` ফোল্ডারের ভেতর থেকে** চালানো হবে। প্রথমে সেখানে যান:

```bash
cd worker
```

তারপর ডিপেন্ডেন্সি ইনস্টল:

```bash
npm install
```

### ৪.১ — ডেটাবেজ তৈরি (D1)

লিখুন:

```bash
wrangler d1 create fcfc-db
```

সফল হলে আউটপুটে **এরকম কিছু** দেখাবে:

```
✅ Successfully created DB 'fcfc-db'
database_id = "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
```

🖊️ **`database_id =`-এর ভেতরের মানটি কপি করুন** (দুই কোটেশনের মাঝের অংশ)।

এখন **VS Code** খুলে `worker` ফোল্ডারের **`wrangler.toml`** ফাইলটি খুলুন। খুঁজুন এই লাইনটা:

```toml
database_id = "REPLACE_WITH_YOUR_D1_DATABASE_ID"
```

`REPLACE_WITH_YOUR_D1_DATABASE_ID` মুছে **কপি করা মানটি** বসান। যেমন:

```toml
database_id = "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
```

ফাইল **সেভ** করুন (Ctrl+S)।

### ৪.২ — KV (ক্যাশ/রেট-লিমিট) তৈরি

লিখুন:

```bash
wrangler kv namespace create KV
```

আউটপুটে এরকম দেখাবে:

```
🌀 Creating namespace with title "fcfc-worker-KV"
✨ Success!
namespace_id = "xyz789abc123..."
```

🖊️ **`namespace_id`-এর মানটি কপি করুন**।

আবার `wrangler.toml`-এ খুঁজুন:

```toml
[[kv_namespaces]]
binding = "KV"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"
```

`REPLACE_WITH_YOUR_KV_NAMESPACE_ID` মুছে কপি করা মান বসান। সেভ করুন।

### ৪.৩ — ফাইল স্টোরেজ বাকেট তৈরি (R2)

লিখুন:

```bash
wrangler r2 bucket create fcfc-media
```

✅ দেখালেই হয়ে গেছে — এখানে আর কিছু কপি করার নেই।

> ⚠️ যদি বলে "R2 not enabled" — তাহলে ব্রাউজারে ক্লাউডফ্লেয়ার ড্যাশবোর্ডে গিয়ে বাম পাশের মেনুতে **R2** ক্লিক করুন, **Purchase R2 Plan** → **Free** প্ল্যান সিলেক্ট করে কনফার্ম করুন। তারপর কমান্ডটা আবার চালান।

### ৪.৪ — ডেটাবেজে টেবিল তৈরি (মাইগ্রেশন)

লিখুন:

```bash
wrangler d1 migrations apply fcfc-db
```

"Are you sure?" জিজ্ঞেস করলে **`y` লিখে Enter** চাপুন।

শেষে `Migration summary` + `Status: Applied` দেখালে টেবিলগুলো তৈরি হয়ে গেছে ✅

---

## ধাপ ৫: সিক্রেট সেট করা

সিক্রেটগুলো হলো সার্ভারের গোপন পাসওয়ার্ড — এগুলো **কাউকে দেবেন না, কোথাও পাবলিক করবেন না**।

### ৫.১ — মূল সিক্রেট দুটি

টার্মিনালে লিখুন (প্রতিবার কমান্ড দেওয়ার পর এটা আপনার কাছে মান চাইবে):

```bash
wrangler secret put JWT_SECRET
```
→ এখানে যেকোনো লম্বা র‍্যান্ডম লেখা দিন (৩০+ অক্ষর)। যেমন: `fcfc-jwt-9f8a7b6c5d4e3f2a1b0c-2026` (এটা হুবহু দেবেন না, নিজের মতো বানান) → Enter।

```bash
wrangler secret put REFRESH_PEPPER
```
→ আরেকটা আলাদা র‍্যান্ডম লেখা দিন → Enter।

> 💡 র‍্যান্ডম লেখা বানাতে: অনলাইনে "random string generator" সার্চ করুন, অথবা কীবোর্ডে এলোমেলো টাইপ করুন। যত লম্বা তত ভালো।

### ৫.২ — পুশ নোটিফিকেশনের কী (VAPID)

লিখুন:

```bash
npm run vapid
```

এরকম **দুটি লাইন** আউটপুট আসবে:

```
# wrangler secret put VAPID_PUBLIC_KEY  → BKx9m... (লম্বা লেখা)
# wrangler secret put VAPID_PRIVATE_KEY → AbC12... (ছোট লেখা)
```

🖊️ দুটো মানই কপি করে রাখুন (নোটপ্যাডে)। তারপর:

```bash
wrangler secret put VAPID_PUBLIC_KEY
```
→ প্রথম লাইনের `→`-এর পরের **পুরো লম্বা লেখাটা** পেস্ট করে Enter।

```bash
wrangler secret put VAPID_PRIVATE_KEY
```
→ দ্বিতীয় লাইনের লেখাটা পেস্ট করে Enter।

### ৫.৩ — পুশ কন্টাক্ট (আপনার ইমেইল)

```bash
wrangler secret put PUSH_CONTACT
```
→ লিখুন: `mailto:আপনার-ইমেইল@জিমেইল.কম` (অবশ্যই শুরুতে `mailto:` থাকবে) → Enter।

### ৫.৪ — কল ফিচার (ঐচ্ছিক — এখন স্কিপ করতে পারেন)

ভয়েস/ভিডিও কল চালাতে চাইলে:

1. ব্রাউজারে ড্যাশবোর্ড: **বাঁ পাশের মেনু → Calls** ক্লিক করুন।
2. **"Create token"** (বা Tokens ট্যাব → Create API Token) ক্লিক করুন → একটা নাম দিন (যেমন `fcfc`) → কপি করুন।
3. টার্মিনালে:
```bash
wrangler secret put CALLS_API_TOKEN
```
→ কপি করা টোকেন পেস্ট করে Enter।

> কল না চালালে এই ধাপ বাদ দিন — বাকি সব কাজ করবে।

### ৫.৫ — যাচাই করুন সব সিক্রেট সেট হয়েছে

```bash
wrangler secret list
```

লিস্টে এই ৬টি নাম দেখা উচিত: `JWT_SECRET`, `REFRESH_PEPPER`, `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `PUSH_CONTACT`, `CALLS_API_TOKEN` (কল স্কিপ করলে ৫টি)।

---

## ধাপ ৬: ব্যাকএন্ড ডিপ্লয় 🚀

টার্মিনালে লিখুন:

```bash
wrangler deploy
```

কিছুক্ষণ পর আউটপুটে এরকম দেখাবে:

```
Published fcfc-backend (x.xx sec)
  https://fcfc-backend.আপনার-নামের-কিছু.workers.dev
Current Deployment ID: ...
```

🖊️ **`https://fcfc-backend.xxxxx.workers.dev` — এই ইউআরএলটা কপি করুন।** এটাই আপনার সার্ভারের ঠিকানা।

### সত্যিই কাজ করছে কি না দেখুন

ব্রাউজারে ওই ইউআরএল খুলুন। এরকম দেখালে সব ঠিক:

```json
{"name":"fcfc","ok":true}
```

❌ এরর পেলে গাইডের শেষের "সমস্যা ও সমাধান" অংশ দেখুন।

---

## ধাপ ৭: ফ্রন্টএন্ডে সার্ভারের ঠিকানা বসানো

এখন **টার্মিনালে ফিরে** `frontend` ফোল্ডারে যান:

```bash
cd ..
cd frontend
```

(`cd ..` মানে এক ঘর উপরে যাওয়া।)

ডিপেন্ডেন্সি ইনস্টল:

```bash
npm install
```

এখন `.env` নামে একটি ফাইল বানাতে হবে:

1. VS Code-এ `frontend` ফোল্ডার খুলুন।
2. দেখবেন `.env.example` নামে একটি ফাইল আছে। সেটা খুলে **ভেতরের সব কপি করুন**।
3. `frontend` ফোল্ডারে **নতুন ফাইল** বানান, নাম দিন ঠিক **`.env`** (শুরুতে ডট, শেষে কোনো এক্সটেনশন নেই)।
4. কপি করা লেখা পেস্ট করুন এবং লাইনটা এমনভাবে বদলান:

```
VITE_API_BASE=https://fcfc-backend.আপনার-করা-নাম.workers.dev
```

অর্থাৎ **ধাপ ৬-তে কপি করা ইউআরএলটা** বসান (শেষে কোনো `/` দেবেন না)। সেভ করুন।

> ⚠️ উইন্ডোজে নাম দিতে সমস্যা হলে: টার্মিনালে লিখুন `cd frontend`, তারপর
> উইন্ডোজ: `copy .env.example .env` | ম্যাক/লিনাক্স: `cp .env.example .env` — তারপর ফাইলটা খুলে এডিট করুন।

---

## ধাপ ৮: ফ্রন্টএন্ড ডিপ্লয় 🚀

টার্মিনালে (নিশ্চিত করুন আপনি `frontend` ফোল্ডারে আছেন):

```bash
npm run build
```

শেষে `✓ built in ...` দেখালে বিল্ড সফল — `dist` নামে একটি ফোল্ডার তৈরি হয়েছে।

এবার প্রথমবারের জন্য পেজেস প্রজেক্ট বানান:

```bash
wrangler pages project create fcfc
```

কিছু জিজ্ঞেস করলে (যেমন প্রোডাকশন ব্রাঞ্চ) — **শুধু Enter চেপে যান** (ডিফল্ট ঠিক আছে)।

এখন ডিপ্লয়:

```bash
wrangler pages deploy dist --project-name=fcfc
```

আউটপুটে এরকম দেখাবে:

```
✨ Deployment complete!
https://fcfc.আরেকটা-নাম.pages.dev
```

🖊️ **এই দ্বিতীয় ইউআরএলটাই আপনার অ্যাপ!** ব্রাউজারে খুলুন — **fcfc** অ্যাপ দেখতে পাবেন 🎉

---

## ধাপ ৯: টেস্ট করা

### ৯.১ — অ্যাকাউন্ট খোলা
1. অ্যাপের ইউআরএল খুলুন → **"সাইনআপ"** ট্যাবে যান।
2. ইউজারনেম লিখলেই ডান পাশে ✅/❌ দেখাবে (রিয়েল-টাইম চেক)।
3. পাসওয়ার্ড দিন (৮+ অক্ষর), **হলুদ বক্সের চেকবক্সে টিক দিন** (এটা ছাড়া সাইনআপ হবে না)।
4. "অ্যাকাউন্ট তৈরি করুন" চাপুন।

### ৯.২ — দ্বিতীয় ডিভাইস টেস্ট (মাল্টি-ডিভাইস অ্যাপ্রুভাল দেখা)
1. ব্রাউজারের **ইনকগনিটো/প্রাইভেট উইন্ডো** খুলে অ্যাপের ইউআরএলে যান।
2. লগইন করুন → দেখাবে **"অনুমোদনের অপেক্ষায়…"**
3. আপনার মূল উইন্ডোতে (যেটা লগইন করা) একটা মডাল পপআপ হবে: **"নতুন ডিভাইস লগইন — Accept/Decline"**
4. **Accept** চাপলে ইনকগনিটো উইন্ডোতে লগইন হয়ে যাবে। (পুশ সাবস্ক্রাইব করা থাকলে নোটিফিকেশনও আসবে।)

### ৯.৩ — চ্যাট টেস্ট
- দুই উইন্ডোতে দুই অ্যাকাউন্ট থেকে একে অপরকে সার্চ করে মেসেজ পাঠান।
- রাইট-ক্লিক মেনু: রিপ্লাই, এডিট, স্টার, পিন, রিয়্যাকশন।
- নিজের মেসেজে **"ডিলিট ফর এভরিওয়ান"** — অন্য উইন্ডোতে মেসেজটা ধোঁয়া হয়ে মিলিয়ে যাবে (Thanos effect)।
- ছবি/ফাইল পাঠান, ভয়েস রেকর্ড করুন (🎤), হেডার থেকে 📞/🎥 কল দিন (কল টোকেন সেট করা থাকলে)।

### ৯.৪ — পুশ নোটিফিকেশন চালু করা
1. অ্যাপে **বাম উপরের গিয়ার আইকন ⚙️ → নোটিফিকেশন**।
2. **"চালু করুন"** বাটনে ক্লিক করুন।
3. ব্রাউজার পারমিশন চাইলে **Allow** দিন।
4. এবার অ্যাপের ট্যাব বন্ধ করে অন্য ডিভাইস থেকে মেসেজ পাঠান — ডেস্কটপে নোটিফিকেশন আসবে।

### ৯.৫ — ইনস্টল করা (PWA)
অ্যাড্রেস বারের ডানে **মনিটর/ডাউনলোড আইকন** দেখালে সেটা ক্লিক করলে অ্যাপ কম্পিউটারে/ফোনে অ্যাপের মতো ইনস্টল হবে।

---

## ধাপ ১০: (ঐচ্ছিক) নিজের ডোমেইন যুক্ত করা

> ⚠️ শর্ত: ডোমেইনটি ক্লাউডফ্লেয়ারে যুক্ত থাকতে হবে (ড্যাশবোর্ড → **Add a site** → ডোমেইন লিখে নেমসার্ভার পরিবর্তন করে)।

ধরা যাক আপনার ডোমেইন `example.com`। পরিকল্পনা:
- অ্যাপ চলবে: `example.com` (বা `app.example.com`)
- এপিআই চলবে: `api.example.com`

### ১০.১ — ওয়ার্কারে (এপিআই) ডোমেইন
1. ড্যাশবোর্ড → বাঁ পাশে **Workers & Pages** ক্লিক করুন।
2. লিস্ট থেকে **`fcfc-backend`** ক্লিক করুন।
3. উপরে **Settings** ট্যাব → বাঁ পাশে **Domains & Routes**।
4. **"+ Add" → "Custom Domain"** ক্লিক করুন।
5. লিখুন: `api.example.com` → **Add Custom Domain**।
6. কিছুক্ষণ পর `https://api.example.com` খুললে `{"name":"fcfc","ok":true}` দেখাবে।

### ১০.২ — পেজেসে (অ্যাপ) ডোমেইন
1. ড্যাশবোর্ড → **Workers & Pages** → **`fcfc`** প্রজেক্ট ক্লিক করুন।
2. **Custom domains** ট্যাব → **"Set up a custom domain"**।
3. লিখুন: `example.com` (বা `app.example.com`) → এগিয়ে যান।

### ১০.৩ — ফ্রন্টএন্ডকে নতুন এপিআই ঠিকানা জানানো
`frontend/.env` ফাইলে এখন লিখুন:

```
VITE_API_BASE=https://api.example.com
```

তারপর টার্মিনালে:

```bash
npm run build
wrangler pages deploy dist --project-name=fcfc
```

ব্যস! এখন `https://example.com` খুললেই আপনার অ্যাপ চলবে। 🎊

> 📌 মনে রাখবেন: `.env` বদলালেই **অবশ্যই** আবার `npm run build` + ডিপ্লয় করতে হয়।

---

## 🖼️ অ্যাপ আইকন বসানো

রিপোতে ইচ্ছাকৃতভাবে কোনো ছবি দেওয়া হয়নি। আপনার বানানো ছবি এখানে বসান:

| ফাইল (পাথ) | সাইজ | কোথায় দেখা যাবে |
|---|---|---|
| `frontend/public/icons/icon-192.png` | 192×192 | PWA আইকন, নোটিফিকেশন আইকন |
| `frontend/public/icons/icon-512.png` | 512×512 | ইনস্টল স্ক্রিন/স্প্ল্যাশ |
| `frontend/public/icons/icon-maskable-512.png` | 512×512 (মাঝে ফাঁকা জায়গা রেখে) | অ্যান্ড্রয়েড আইকন |
| `frontend/public/icons/apple-touch-icon.png` | 180×180 | আইফোন হোম স্ক্রিন |

বসানোর পর আবার `npm run build` + `wrangler pages deploy dist --project-name=fcfc`।

> ছবি বানাতে: যেকোনো ডিজাইন টুল, অথবা অনলাইনে "maskable icon generator" ব্যবহার করুন।

---

## 🔁 ভবিষ্যতে কোড বদলালে আবার ডিপ্লয়

**শুধু ব্যাকএন্ড বদলালে** (`worker/` ফোল্ডার):
```bash
cd worker
wrangler deploy
```

**শুধু ফ্রন্টএন্ড বদলালে** (`frontend/` ফোল্ডার):
```bash
cd frontend
npm run build
wrangler pages deploy dist --project-name=fcfc
```

**ডেটাবেজের স্ট্রাকচার বদলালে** (নতুন মাইগ্রেশন ফাইল যোগ করলে):
```bash
cd worker
wrangler d1 migrations apply fcfc-db
```

---

## 🩺 সমস্যা ও সমাধান (ট্রাবলশুটিং)

| সমস্যা | কারণ ও সমাধান |
|---|---|
| `wrangler: command not found` | `npm install -g wrangler` আবার চালান; নোড ইনস্টল ঠিক আছে কি না দেখুন। |
| `You are not authenticated` | `wrangler login` চালিয়ে ব্রাউজারে Allow করুন। |
| `REPLACE_WITH_YOUR_D1_DATABASE_ID` এরর | ধাপ ৪.১/৪.২-এর আইডিগুলো `wrangler.toml`-এ বসাতে ভুলে গেছেন। |
| ডিপ্লয়ের পর `{"name":"fcfc","ok":true}` আসে না | ২-১ মিনিট অপেক্ষা করে রিফ্রেশ করুন; না হলে ড্যাশবোর্ড → Workers → fcfc-backend → Logs দেখুন। |
| অ্যাপ খোলে কিন্তু লগইন কাজ করে না / কনসোলে 401 | `frontend/.env`-এর ইউআরএল ভুল — ধাপ ৬-এর `workers.dev` ঠিকানা হুবহু বসান, শেষে `/` নয়; আবার বিল্ড+ডিপ্লয় করুন। |
| অ্যাপ খোলে, লগইন হয়, কিন্তু মেসেজ আসে না | সাধারণত একই কারণ (সার্ভার ঠিকানা ভুল) — ব্রাউজার কনসোলে (F12) লাল এরর দেখুন। |
| পুশ নোটিফিকেশন আসে না | ১) সেটিংস থেকে "চালু করুন" চাপা হয়েছে কি? ২) ব্রাউজার সাইট-সেটিংসে নোটিফিকেশন Allow আছে কি? ৩) `http://localhost`-এ পুশ কাজ করে না — ডিপ্লয় করা সাইটে টেস্ট করুন। |
| ভিডিও কলে "কল শুরু করা যায়নি" | `CALLS_API_TOKEN` সেট করা হয়নি (ধাপ ৫.৪)। টোকেন দিয়ে আবার চেষ্টা করুন। |
| সাইনআপে "username taken" | ওই নাম আগে কেউ নিয়েছে — অন্য নাম দিন। |
| পাসওয়ার্ড ভুলে গেছি! | রিকভারি **নেই** — এটাই ডিজাইন। নতুন অ্যাকাউন্ট খুলুন। |
| `X25519`/ক্রিপ্টো এরর | ব্রাউজার আপডেট করুন (ক্রোম/এজ/ফায়ারফক্স সর্বশেষ)। |
| লোকালে (`npm run dev`) টেস্ট করতে চাই | রিডমির "লোকাল ডেভেলপমেন্ট" অংশ দেখুন — `worker/.dev.vars` ফাইল বানাতে হবে। |

---

## ✅ সবশেষে চেকলিস্ট

- [ ] `wrangler.toml`-এ D1 ও KV আইডি বসানো
- [ ] `wrangler d1 migrations apply fcfc-db` সফল
- [ ] ৬টি সিক্রেট সেট (`wrangler secret list` দিয়ে যাচাই)
- [ ] `wrangler deploy` → `{"name":"fcfc","ok":true}` দেখা যাচ্ছে
- [ ] `frontend/.env`-এ সার্ভার ইউআরএল বসানো
- [ ] `wrangler pages deploy dist` → অ্যাপ ইউআরএল খুলছে
- [ ] সাইনআপ → লগইন → দুই ডিভাইসে অ্যাপ্রুভাল টেস্ট সফল
- [ ] পুশ নোটিফিকেশন "চালু করুন" চাপা
- [ ] (ঐচ্ছিক) আইকন ফাইল বসানো, কাস্টম ডোমেইন যুক্ত

সব টিক পড়লে — **অভিনন্দন, আপনার `fcfc` লাইভে!** 🎉
